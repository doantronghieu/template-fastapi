"""Core application prompts.

Store as .md files. Organize in subdirectories (e.g., common/, features/).

Usage:
    from app.resources.llm import load_prompt
    prompt = load_prompt("common/system")
"""
