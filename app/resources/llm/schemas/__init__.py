"""LLM structured output schemas.

This module contains both Pydantic models and JSON schemas for LLM structured outputs.

Store as .json files.

Usage:
    from app.resources.llm import load_schema
    schema = load_schema("example")
"""
